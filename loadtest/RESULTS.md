# Load test results

Generated by `python loadtest/report.py` from `loadtest/results/`. Each level ran 45 s with Locust *inside* the kind cluster (see [README](README.md) for why). SLO: p95 under 500 ms with under 1% errors, over the whole traffic mix.

- Whole-mix ceiling (p95 < 500 ms), `baseline`: **none**
- Whole-mix ceiling (p95 < 500 ms), `after-fix`: **none**
- Whole-mix ceiling (p95 < 500 ms), `after-wait-budget`: **none**

## Ceiling per request class

Total throughput at the highest load where every request of that class met its objective with under 1% errors.

| Class (p95 objective) | `baseline` | `after-fix` | `after-wait-budget` |
|---|---:|---:|---:|
| chat (< 500 ms) | 20.6 req/s (25 users) | 68.1 req/s (100 users) | 153.7 req/s (200 users) |
| rag (< 1000 ms) | 20.6 req/s (25 users) | none | 79.0 req/s (100 users) |

## Throughput and errors

| Users | baseline | after-fix | after-wait-budget |
|---:|---:|---:|---:|
| 25 | 20.6 req/s | 17.5 req/s | 20.4 req/s |
| 50 | 14.5 req/s, **74.46% errors** | 29.1 req/s | 41.2 req/s |
| 100 | 21.5 req/s, **81.02% errors** | 68.1 req/s | 79.0 req/s |
| 200 | 22.9 req/s, **88.28% errors** | 127.8 req/s | 153.7 req/s |
| 400 | 15.0 req/s, **90.92% errors** | 182.4 req/s, **13.76% errors** | 203.4 req/s, **11.64% errors** |

## p95 latency, whole traffic mix

| Users | baseline | after-fix | after-wait-budget |
|---:|---:|---:|---:|
| 25 | 690 ms | 1600 ms | 550 ms |
| 50 | 2700 ms | 3200 ms | 740 ms |
| 100 | 19000 ms | 3700 ms | 840 ms |
| 200 | 19000 ms | 1000 ms | 770 ms |
| 400 | 5900 ms | 2100 ms | 1900 ms |

## By request kind: `after-wait-budget` at 200 users

| Request kind | req/s | p50 | p95 | p99 | errors |
|---|---:|---:|---:|---:|---:|
| chat: cache hit | 57.1 | 110 ms | 430 ms | 1200 ms | 0.0% |
| chat: provider call | 55.6 | 120 ms | 200 ms | 690 ms | 0.0% |
| rag: answer | 14.4 | 500 ms | 1300 ms | 1700 ms | 0.0% |
| rag: search | 26.6 | 390 ms | 1300 ms | 1600 ms | 0.0% |

## Cache and cost (`after-wait-budget`, all levels)

- Cache hit rate: 37.2% of 19410 requests
- Cost per 1,000 requests: $0.071078; saved by the cache per 1,000: $0.020352
- Costs use the mock route's pricing (priced like a small hosted model) and its word-count token estimates, so they compare configurations; they are not a bill.

## Chaos test

A steady 20 req/s to the `mock` route with the cache off. At 15s the primary deployment was made to fail every call (through the admin fault-injection API, on every replica); at 60s the fault was cleared.

| Phase | Requests | Succeeded | Served by primary | p50 | p95 | Cost / 1k requests |
|---|---:|---:|---:|---:|---:|---:|
| healthy | 300 | 100.0% | 100.0% | 83 ms | 84 ms | $0.027 |
| fault | 900 | 100.0% | 1.3% | 153 ms | 155 ms | $0.13356 |
| recovery | 900 | 100.0% | 64.4% | 84 ms | 154 ms | $0.0654 |

- **User-visible failures: 0.** Every request during the fault was answered, by the fallback.
- **Breakers opened 0.9 s after the fault**, after 7 failed primary calls, all absorbed by fallback: 3 per replica (the breaker threshold), plus one already in flight. The first failure lands up to ~1 s after injection because each replica caches the fault table for a second.
- 2 failed half-open probes while the primary stayed down: one per replica per 30 s cooldown, checking whether it had recovered.
- **Primary serving again 15.95 s after the fix**: the breakers re-check once per cooldown, so recovery takes up to 30 s after a fix.
- Failed primary calls, seconds after the fault: [0.6, 0.65, 0.7, 0.75, 0.8, 0.85, 0.9, 30.9, 31.0]

## Autoscaling

`python loadtest/autoscale.py` queues a burst of uploads, then samples queue depth, what the worker HPA reads, and the replica count every 5 s until the backlog clears. Raw data: `results/autoscale*.json`.

| Run | Burst | Workers | Peak queue | Scaled out at | Backlog cleared |
|---|---|---:|---:|---:|---:|
| one worker (control) | 150 × 160 KB | 1, pinned | 65 | — | **312 s** |
| autoscaled | 150 × 160 KB | 1 → 4 | 67 | 63 s | **324 s** |
| one worker (control) | 300 × 24 KB | 1, pinned | 58 | — | 108 s |
| autoscaled | 300 × 24 KB | 1 → 3 | 58 | 77 s | 109 s |

**The control loop works.** Queue depth reached 51 jobs per worker against a target of 5, the HPA added pods, and the queue drained to zero. Scale-out takes about 63 s: a deliberate 60 s stabilisation window, plus pod start. Nothing scales on an empty queue any more.

**On one node it does not pay for itself, and the honest number says so:** 324 s with four workers against 312 s with one. A single ingest job already uses about 8 cores, so the extra pods competed for CPUs that were busy, and each one paid a model-loading cost on the way in. With 24 KB documents the two runs were also a dead heat (108 s vs 109 s), for a different reason: uploads arrived no faster than one worker cleared them, so there was never a real backlog.

That is a statement about a 12-vCPU laptop, not about the design. A worker's CPU request was raised from 250m to 1000m to stop the scheduler packing workers onto a node that one of them saturates, the kind overlay caps the worker HPA at 2 replicas, and the base keeps 4 for a cluster with somewhere to put them.

**The same test found two bugs** (details in [README](README.md#autoscaling)): a document large enough to OOM-kill the worker embedding it, and the poison pill it then became — redelivered after each kill, one job crash-looped four workers for 70 minutes. Both are fixed and covered by tests; the burst that caused them now completes with zero restarts.
